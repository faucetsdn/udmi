{
  "title": "Properties",
  "type": "object",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "required": [
    "key_type",
    "version",
    "connect"
  ],
  "properties": {
    "key_type": {
      "enum": [
        "RSA_PEM",
        "RSA_X509_PEM"
      ]
    },
    "version": {
      "enum": [
        1
      ]
    },
    "connect": {
      "enum": [
        "direct"
      ]
    }
  }  
}
