{
  "$id": "https://raw.githubusercontent.com/faucetsdn/udmi/1.4.0/schema/state_pointset_point.json",
  "title": "Point Pointset State",
  "description": "Object representation for for a single point",
  "type": "object",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
    "units": {
      "description": "If specified, indicates a programmed point unit. If empty, means unspecified or matches configured point.",
      "type": "string"
    },
    "value_state": {
      "description": "Optional enumeration indicating the state of the points value.",
      "enum": [
        "applied",
        "updating",
        "overridden",
        "invalid",
        "failure"
      ]
    },
    "status": {
      "description": "Optional status information about this point, subject to log severity level",
      "$ref": "file:common.json#/definitions/entry"
    }
  }
}
