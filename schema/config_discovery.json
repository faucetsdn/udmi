{
  "$udmi_version": "1.4.0",
  "title": "Discovery Config",
  "description": "Configuration for [discovery](../docs/specs/discovery.md)",
  "type": "object",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
    "generation": {
      "description": "Generational marker for controlling enumeration",
      "type": "string",
      "format": "date-time"
    },
    "enumerate": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "networks": {
          "type": "boolean"
        },
        "uniqs": {
          "type": "boolean"
        },
        "features": {
          "type": "boolean"
        }
      }
    },
    "networks": {
      "description": "Address network config for a scan. Not included for device enumeration messages.",
      "existingJavaType": "java.util.HashMap<String, NetworkDiscoveryConfig>",
      "additionalProperties": false,
      "patternProperties": {
        "^iot|bacnet|ipv4|ipv6|ethmac$": {
          "$ref": "file:config_discovery_network.json"
        }
      }
    }
  }
}
