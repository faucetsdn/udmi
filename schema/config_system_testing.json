{
  "$udmi_version": "1.5.2",
  "title": "Testing System Config",
  "description": "Configuration parameters for device-under-test",
  "type": "object",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
    "sequence_name": {
      "description": "The sequence name currently being tested (for debug logging)",
      "type": "string"
    },
    "transaction_id": {
      "description": "The transaction id used to generate this config update",
      "type": "string"
    },
    "config_base": {
      "description": "The configuration version that this update was based on",
      "type": "integer"
    },
    "endpoint_type": {
      "description": "Designator for the kind of endpoint being used for this test",
      "type": "string"
    }
  }
}
