{
  "$udmi_version": "1.5.2",
  "title": "Point Pointset Events",
  "description": "Object representation for for a single point",
  "type": "object",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
    "present_value": {
      "description": "The specific point data reading.  If the value is numeric, then the type must be integer or number.  If the value is an integer, it should be represented as type integer",
       "oneOf": [
       	 { "type": "string" },
         { "type": "boolean" },
       	 { "type": "number" }		
       ],
      "examples": [24.1, "running", 4]
    }
  },
  "required": [
    "present_value"
  ]
}
