{
  "$udmi_version": "1.5.7",
  "title": "Trace Discovery Config",
  "description": "Configuration parameters for trace-level discovery operations (when depth is 'trace')",
  "type": "object",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
    "interface": {
      "description": "Network interface to target for trace capture operations",
      "type": "string"
    },
    "filter": {
      "description": "Protocol or packet filter expression (e.g. BPF filter)",
      "type": "string"
    },
    "max_bytes": {
      "description": "Maximum byte volume cap for trace collection to prevent exhaustion",
      "type": "integer",
      "minimum": 0
    }
  }
}
