#!/bin/bash -e

if [[ $# != 1 ]]; then
    echo Usage: $0 project_id
    false
fi

project_id=$1
shift

ROOT=$(dirname $0)/..
cd $ROOT

bin/setup $project_id

bin/container prep
bin/container build
bin/container push
bin/container deploy
sleep 10
kubectl get pods
