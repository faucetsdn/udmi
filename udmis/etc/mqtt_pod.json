{
  "include": "prod_pod.json",
  "flow_defaults": {
    "protocol": "mqtt",
    "hostname": "localhost"
  },
  "flows": {
    "provider": {
      "recv_id" : "${UDMI_PREFIX}udmi_target-discovery",
      "send_id" : "${UDMI_PREFIX}udmi_reflect",
      "side_id" : "${UDMI_PREFIX}udmi_state"
    }
  },
  "iot_access": {
    "UDMI-REFLECT": {
      "provider": "implicit"
    }
  }
}
