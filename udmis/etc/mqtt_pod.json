{
  "include": "prod_pod.json",
  "flow_defaults": {
    "protocol": "mqtt",
    "hostname": "localhost"
  },
  "iot_access": {
    "implicit": {
      "provider": "implicit",
      "options": "enabled=${ETCD_CLUSTER}"
    }
  }
}
