{
  "include": "prod_pod.json",
  "flow_defaults": {
    "protocol": "mqtt",
    "transport": "ssl",
    "hostname": "localhost",
    "auth_provider": {
      "basic": {
        "username": "rocket",
        "password": "monkey"
      }
    }
  },
  "flows": {
    "state": {
      "recv_id": "/r/{registryId}/d/{deviceId}/state",
      "send_id": "/r/{registryId}/d/{deviceId}/{subType}/{subFolder}"
    },
    "distributor": {
      "enabled": "false"
    }
  },
  "iot_access": {
    "implicit": {
      "provider": "implicit",
      "options": "enabled=${ETCD_CLUSTER}"
    }
  }
}
