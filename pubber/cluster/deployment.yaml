apiVersion: apps/v1
kind: Deployment
metadata:
  name: pubber-pool
spec:
  selector:
    matchLabels:
      app: pubber
  replicas: 500
  strategy:
    rollingUpdate:
      maxSurge: 50%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: pubber
    spec:
      containers:
      - name: pubber
        image: "us.gcr.io/bos-udmi-hub/pubber:latest"
        imagePullPolicy: Always
        env:
          - name: PUBBER_INSTANCES
            value: "20"
        resources:
          requests:
            memory: 250Mi
            cpu: 10m
