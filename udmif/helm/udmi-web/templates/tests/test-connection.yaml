apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "udmi-web.fullname" . }}-test-connection"
  labels:
    {{- include "udmi-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "udmi-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
