package udmi.schema;

import java.util.HashMap;
import java.util.Map;

// This class is manually curated and then copied into the gencode directory. Look for the
// proper source and don't be fooled! These are generally useful constants.
public static class Predefined {
  public static final String IOT_ENDPOINT_BLOB_NAME = "_iot_config";
}
