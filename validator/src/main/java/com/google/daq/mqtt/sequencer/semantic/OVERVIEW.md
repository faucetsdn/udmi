# Functional Overview

Defines the semantic evaluation rules for the sequencer, allowing tests to verify not just JSON structure, but logical constraints (e.g., 'temperature must decrease after HVAC starts').
