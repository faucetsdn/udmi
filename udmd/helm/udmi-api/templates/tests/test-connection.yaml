apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "udmi-api.fullname" . }}-test-connection"
  labels:
    {{- include "udmi-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "udmi-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
